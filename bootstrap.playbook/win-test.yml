---
- hosts: winbots
    gather_facts: yes
    become: yes
    become_method: sudo
    tasks:
    
# Install/uninstall with chocolatey
    - name: Ensure 7-Zip is installed via Chocolatey
      win_chocolatey:
        name: 7zip
        state: present